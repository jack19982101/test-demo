import { __assign } from "tslib";
import { registerShape, Util } from '@antv/g2';
registerShape('point', 'word-cloud', {
    draw: function (cfg, group) {
        var cx = cfg.x;
        var cy = cfg.y;
        var shape = group.addShape('text', {
            attrs: __assign(__assign({}, getTextAttrs(cfg)), { x: cx, y: cy }),
        });
        var rotate = cfg.data.rotate;
        if (rotate) {
            Util.rotate(shape, (rotate * Math.PI) / 180);
        }
        return shape;
    },
});
function getTextAttrs(cfg) {
    var _a;
    return __assign(__assign(__assign({}, cfg.defaultStyle), cfg.style), { fontSize: cfg.data.size, text: cfg.data.text, textAlign: 'center', fontFamily: cfg.data.font, fontWeight: cfg.data.weight, fill: cfg.color || ((_a = cfg.defaultStyle) === null || _a === void 0 ? void 0 : _a.stroke), textBaseline: 'alphabetic' });
}
//# sourceMappingURL=word-cloud.js.map
import { __assign, __extends } from "tslib";
import { deepMix } from '@antv/util';
import { getPercentTooltipTemplate } from '../../utils/percent-tooltip';
import { Plot } from '../../core/plot';
import { adaptor } from './adaptor';
/**
 * 条形图
 */
var Bar = /** @class */ (function (_super) {
    __extends(Bar, _super);
    function Bar() {
        var _this = _super !== null && _super.apply(this, arguments) || this;
        /** 图表类型 */
        _this.type = 'bar';
        return _this;
    }
    /**
     * 获取 条形图 默认配置
     */
    Bar.prototype.getDefaultOptions = function (options) {
        var isPercent = options.isPercent, isRange = options.isRange, label = options.label, xField = options.xField;
        return deepMix({}, _super.prototype.getDefaultOptions.call(this), {
            label: label && isRange
                ? __assign({ content: function (item) {
                        var _a;
                        return (_a = item[xField]) === null || _a === void 0 ? void 0 : _a.join('-');
                    } }, label) : label,
            tooltip: {
                shared: true,
                showMarkers: false,
                offset: 20,
                customContent: isPercent
                    ? function (value, items) {
                        return getPercentTooltipTemplate(value, items);
                    }
                    : '',
            },
            interactions: [{ type: 'active-region' }],
        });
    };
    /**
     * 获取 条形图 的适配器
     */
    Bar.prototype.getSchemaAdaptor = function () {
        return adaptor;
    };
    return Bar;
}(Plot));
export { Bar };
//# sourceMappingURL=index.js.map
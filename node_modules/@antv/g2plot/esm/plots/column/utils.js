import DataSet from '@antv/data-set';
/**
 * percent 处理数据
 * @param options
 */
export function transformData(options) {
    var data = options.data, xField = options.xField, seriesField = options.seriesField, yField = options.yField;
    var DataView = DataSet.DataView;
    var dv = new DataView();
    dv.source(data).transform({
        type: 'percent',
        field: yField,
        dimension: seriesField,
        groupBy: [xField],
        as: yField,
    });
    return dv.rows;
}
//# sourceMappingURL=utils.js.map
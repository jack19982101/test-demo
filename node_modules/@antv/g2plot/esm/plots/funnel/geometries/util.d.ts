import { Params } from '../../../core/adaptor';
import { FunnelAdaptorOptions } from '../types';
/**
 * 转置处理, 适用于普通折线图，动态高度折线图
 * @param params
 */
export declare function transpose(params: Params<FunnelAdaptorOptions>): Params<FunnelAdaptorOptions>;

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.line = void 0;
var tslib_1 = require("tslib");
var util_1 = require("@antv/util");
var base_1 = require("./base");
/**
 * line 辅助点的配置处理
 * @param params
 */
function line(params) {
    var options = params.options;
    var line = options.line, stepType = options.stepType, seriesField = options.seriesField, smooth = options.smooth, connectNulls = options.connectNulls;
    // 如果存在才处理
    return line
        ? base_1.geometry(util_1.deepMix({}, params, {
            options: {
                type: 'line',
                colorField: seriesField,
                mapping: tslib_1.__assign({ shape: stepType || (smooth ? 'smooth' : 'line') }, line),
                args: { connectNulls: connectNulls },
            },
        }))
        : params;
}
exports.line = line;
//# sourceMappingURL=line.js.map
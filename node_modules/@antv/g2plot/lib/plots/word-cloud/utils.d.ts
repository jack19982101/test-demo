import { Params } from '../../core/adaptor';
import { DataItem, WordCloudOptions } from './types';
/**
 * 用 DataSet 转换词云图数据
 * @param params
 */
export declare function transform(params: Params<WordCloudOptions>): DataItem[];
/**
 * 处理 imageMask 可能为 url 字符串的情况
 * @param img
 * @param callback
 */
export declare function processImageMask(img: HTMLImageElement | string, callback?: (img?: HTMLImageElement) => void): void;

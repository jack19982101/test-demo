import { IGroup, IShape } from '@antv/g-base';
/**
 * 对元素设置一些操作（旋转、移动等），每次重置 matrix
 * @param group
 * @param actions
 */
export declare function groupTransform(group: IGroup | IShape, actions: any[][]): void;
/**
 * 矩阵变换
 * @param actions
 * @param matrix
 */
export declare function transform(actions: any[][], matrix?: number[]): number[];
